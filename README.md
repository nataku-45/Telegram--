# 🎙️ AI Voice Transcriber Telegram Bot

Этот бот использует `aiogram` для взаимодействия с пользователями Telegram и Gemini API (`google-generativeai`) для обработки голосовых сообщений. Бот принимает аудио, транскрибирует его и выделяет ключевые пункты.

## ⚙️ Технологический Стек

*   **Python:** 3.12 (Требуется, так как более новые версии могут вызывать конфликты с компилируемыми зависимостями, такими как `pydantic-core`).
*   **Бот-фреймворк:** `aiogram` (версия 3.x).
*   **AI Модель:** Google Gemini 2.5 Flash.
*   **Управление окружением:** `python-dotenv`.

## 🚀 Настройка и Запуск

Для успешного запуска бота необходимо выполнить следующие шаги:

### 1. Получение Ключей API

Для работы бота вам потребуется два токена:

1.  **Telegram Bot Token:** Получите у [@BotFather](https://t.me/BotFather) в Telegram.
2.  **Gemini API Key:** Получите в Google AI Studio.

### 2. Создание и Конфигурация Файлов

Убедитесь, что в корневом каталоге проекта присутствуют следующие файлы:

#### `requirements.txt`
Содержит необходимые зависимости:
```
aiogram>=3.0.0
google-generativeai
python-dotenv
requests
```

#### `.env`
Заполните файл `.env` вашими реальными токенами:

```ini
TELEGRAM_BOT_TOKEN="ВАШ_ТОКЕН_TELEGRAM"
GEMINI_API_KEY="ВАШ_API_КЛЮЧ_GEMINI"
```

### 3. Установка Зависимостей

**ВАЖНО:** Из-за проблем с совместимостью библиотек (в частности `pydantic-core`) установка должна производиться с использованием Python версии 3.12.

Выполните в терминале:

```bash
py -3.12 -m pip install -r requirements.txt
```

### 4. Запуск Бота

Запустите бота, явно указав Python 3.12:

```bash
py -3.12 main.py
```

---

### Примечание по Геоблокировке (Важно)

Если вы столкнетесь с ошибкой `User location is not supported for the API use.`, это означает, что Google блокирует запросы с IP-адреса, где запущен скрипт. Для надежной работы бота **рекомендуется развернуть его на удаленном сервере (VPS) в регионе, где доступен Gemini API** (например, США или Западная Европа).